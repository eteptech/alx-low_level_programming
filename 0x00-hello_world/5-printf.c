#include <stdio.h>
/**
 * main - printf this with "proper grammar, but the outcome is a piece of art,
 * Return: 0 if the program excutes correctly
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
