#include <stdio.h>
/**
 *main - Print hexadecimal value,
 *Return: 0 on SUCCESS
 */
int main(void)
{
	int i;
	char chr;

	for (n = 48; n < 58; n++)
		putchar(n);
	for (ch = 'a'; ch < 'g')
		putchar(ch);
	putchar('\n');
	return (0);
}

